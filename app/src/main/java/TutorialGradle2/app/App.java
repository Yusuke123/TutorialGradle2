/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package TutorialGradle2.app;

import TutorialGradle2.list.LinkedList;

import static TutorialGradle2.utilities.StringUtils.join;
import static TutorialGradle2.utilities.StringUtils.split;
import static TutorialGradle2.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
